"""webllm-agent — local coding-agent runtime over web LLMs.

The browser is a transport, the provider web app is an adapter,
the backend is an LLM abstraction. The agent engine must not
know Playwright exists.
"""

__version__ = "0.1.0"
